# 待办事项总清单

> 最后更新：2026-06-07
> 分支：codex-dual-wallet-v1
> 最新 commit：483c293

---

## 一、已完成的工作

### 安全加固
- 旧 PaymentController 生产禁用（@Profile + @ConditionalOnProperty）
- CORS 配置外部化（@Value 从 application.yml 读取）
- 支付验签真实实现 PaymentSignatureVerifierImpl（@Primary），Stub 改为 @Profile("dev") + fail-closed
- 登录失败锁定机制（LoginFailRecord + LoginSecurityService，5次/30分钟，原子更新，唯一约束）
- V1 登录接口限流（@RateLimit 注解覆盖 password/sms/third-party/merchant 四端）
- 10+ 项 CRITICAL/HIGH 安全漏洞修复（注册提权、支付回调验签、水平越权等）
- AuthGuard/RoleGuard 全角色服务端 session 验证
- 全项目 e.getMessage() 错误信息脱敏（20+ 处）

### 后端架构
- Entity → VO 转换层（8 个 VO：订单/账单/商户/商品/充值/积分/退款）
- VoConverterUtil 公共工具类提取
- 旧版 Controller 补 @SaCheckLogin（OrderController/ProductController/V1MerchantMarketingController）
- OrderConsumer 实现（积分发放 + 会员自动升级）
- 会员自动升级逻辑（checkAndAutoUpgrade + PaymentV1Consumer 集成）
- 积分兑换退款回退（PointsService.refundPoints + RefundApplicationService.completeRefund）
- 优惠券叠加策略（StackStrategyEnum + 定价引擎策略分支）
- AiController 类名拼写修正 + /api 重复前缀修复
- OrderConsumer 改 @RequiredArgsConstructor 构造器注入
- 52 个测试文件（含 19 个新增用例覆盖登录锁定/退款/OrderConsumer）

### 退款/售后模块
- RefundApplication Entity + Mapper + Service + DTO + VO + 枚举
- V1AppRefundController（用户端 4 接口：申请/列表/详情/取消）
- V1MerchantRefundController（商户端 2 接口：列表/审核）
- 建表 SQL + 唯一约束防并发

### 前端页面
- 路由守卫：AuthGuard / RoleGuard / GuestGuard（三层守卫 + 全路由改造）
- 用户端：CouponCenter（优惠券中心）、Points（积分中心）、ApplyRefund（退款申请）
- 商户端：MerchantCoupons / MerchantActivities / MerchantMembers / MerchantRefunds
- 管理端：AdminMarketing + AdminCouponsTab + AdminActivitiesTab
- 优惠券下单集成（Cart.tsx 选择器 + orderCheckout 传递 couponId）
- Discovery 搜索防抖 + keyword URL 参数
- AdminProducts 接真数据（appCatalogService）
- Profile 死链路修复（Toast "即将上线"）
- Home 分类入口功能化（跳转 Discovery 带 category 参数）
- ToastContext 通知组件 + EmptyState 公共组件

### 文档
- `docs/开发进度报告_20260607.md`
- `docs/项目架构全景文档.md`
- `docs/frontend-page-specs.md` / `wave2.md` / `wave3.md`

---

## 二、当前真实待办

### P1 — 核心功能补全

#### #5 短信验证码真实实现
- **端**：全栈
- **工作量**：后端 2-3 天 + 前端 1 天
- **现状**：Login.tsx 短信登录仍复用 password 字段（line 115 明确注释）。SmsCodeService 接口和 SmsAuthProperties 配置已有，但未接入真实短信服务商
- **需要做**：
  - 后端：接入阿里云/腾讯云短信 SDK，实现 SmsCodeService 发送和校验逻辑
  - 前端：Login.tsx 短信 Tab 增加验证码输入框 + 60秒倒计时 + 发送按钮
- **依赖**：需开通短信服务商账号

#### #32 退款渠道支持补全
- **端**：后端
- **工作量**：2-3 天
- **现状**：RefundServiceImpl:146 标注 "not supported in phase 1"。退款申请/审核流程已完成，但审核通过后未实际调用微信/支付宝退款 API
- **需要做**：RefundApplicationService.completeRefund 与支付渠道退款对接
- **依赖**：微信/支付宝退款商户证书配置

### P2 — 运营能力

#### #27 数据分析可视化
- **端**：前端
- **工作量**：3-5 天
- **现状**：DataAnalysisController 有框架但无前端页面
- **需要做**：新建数据看板页面，接入图表库（ECharts/Recharts），展示销售趋势、用户增长、商品排行、营收分析
- **后端已就绪**：DataAnalysisController + SalesStatisticsController

#### #28 AdminAnalytics 接真数据
- **端**：前端
- **工作量**：2-3 天
- **现状**：AdminAnalytics.tsx 仍是本地数组硬编码假图表（line 24）
- **需要做**：替换假数据为 API 调用（V1AdminDashboardController + SalesStatisticsController）

### P3 — 业务扩展

#### #25 门店主数据管理
- **端**：全栈
- **工作量**：3-5 天
- **现状**：DB 已有 store 表（含经纬度、营业时间等），但前后端仍围绕 tenant 级别操作
- **需要做**：商品表增加 store_id、订单表增加 store_id、前端门店选择器
- **前置条件**：确认业务是否需要多门店模型

### P4 — 架构完善

#### #24 RabbitMQ 补偿扩展
- **端**：后端
- **工作量**：3-5 天
- **现状**：优惠券/短信/第三方/AI 的失败补偿未通过 MQ。已有 Outbox + CompensationTask + DeadLetterTask 基础设施
- **需要做**：将各 Service 的异步操作统一接入 Outbox 模式

#### #26 AI 助手真实联调
- **端**：全栈
- **工作量**：5-7 天
- **现状**：AiController 全部返回 disabled；前端 AIAssistant.tsx 用 setTimeout 模拟。LangChain4j + 通义千问配置已有（Aiconfig、AiTools、RedisChatMemoryStore）
- **需要做**：启用 AI 接口、实现商品推荐/智能客服、前端接入流式响应
- **依赖**：AI API Key 已配置，需确认额度

---

## 三、建议执行顺序

```
第一波（1 周）：
  #28 AdminAnalytics 接真数据（2-3天，前端独立，后端 API 已就绪）
  #32 退款渠道支持补全（2-3天，后端独立）

第二波（1-2 周）：
  #5 短信验证码真实实现（3-4天，需短信服务商账号）
  #27 数据分析可视化（3-5天）

第三波（按需）：
  #25 门店主数据管理（需确认业务需求）
  #24 RabbitMQ 补偿扩展（架构优化）
  #26 AI 助手真实联调（差异化功能）
```

---

## 四、测试账号

密码统一：`admin123`

| 角色 | 用户名 | 手机 | 说明 |
|------|--------|------|------|
| 管理员 | admin | 13800000001 | sys_user + platform_user 双表 |
| 商户 | merchant | 13800000002 | tenant_employee 关联 tenant 1 |
| 用户 | user | 13800000003 | 500 积分 + 2 笔测试订单 |
| 用户2 | user2 | 13800000004 | 100 积分，测试越权用 |

初始化顺序：`99_init_data.sql` → `99_test_accounts.sql`

---

## 五、关键文件路径

| 文件 | 用途 |
|------|------|
| `docs/开发进度报告_20260607.md` | Wave 1-3 完整记录 |
| `docs/项目架构全景文档.md` | 项目结构 + 数据库分析 |
| `docs/待办事项清单.md` | 待办详细说明（含涉及文件和依赖） |
| `payment-system/src/main/java/com/payment/service/impl/PaymentSignatureVerifierImpl.java` | 支付验签真实实现 |
| `payment-system/src/main/java/com/payment/service/impl/PaymentSignatureVerifierStub.java` | 支付验签桩（dev only） |
| `payment-system/src/main/java/com/payment/service/impl/OrderPricingServiceImpl.java` | 订单定价引擎（含叠加策略） |
| `payment-system/src/main/java/com/payment/service/impl/LoginSecurityServiceImpl.java` | 登录安全服务 |
| `payment-system/src/main/java/com/payment/service/impl/RefundApplicationServiceImpl.java` | 退款申请服务 |
| `payment-system/src/main/java/com/payment/vo/VoConverterUtil.java` | VO 转换工具类 |
| `salessystem/src/components/guards/` | 路由守卫组件 |
| `salessystem/src/services/modules/` | 前端 API Service 模块 |
| `payment-system/sql/99_test_accounts.sql` | 测试账号数据 |
