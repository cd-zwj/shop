# 支付系统快速启动指南

## 前置要求

- Java 8+
- Maven 3.6+
- Node.js 16+
- Nginx
- MySQL 5.7+
- Redis 5.0+

## 开发环境启动

### 1. 启动基础服务

#### MySQL
```bash
# 导入数据库脚本（多租户版本）
mysql -u root -p < payment-system/sql/payment_db_multitenant.sql

# 或者单租户版本
# mysql -u root -p < payment-system/sql/payment_db.sql
```

数据库脚本位置：
- 多租户版本（推荐）: `payment-system/sql/payment_db_multitenant.sql`
- 单租户版本: `payment-system/sql/payment_db.sql`

#### Redis
```bash
redis-server
```

### 2. 启动后端服务

```bash
cd payment-system
mvn spring-boot:run
```

或者：
```bash
mvn clean package -DskipTests
java -jar target/payment-system-1.0.0.jar
```

后端服务运行在: http://localhost:8080

### 3. 启动前端（开发模式）

#### 商家管理后台
```bash
cd payment-frontend-admin
npm install
npm run dev
```
访问: http://localhost:5173

#### 用户端应用
```bash
cd payment-frontend-user
npm install
npm run dev
```
访问: http://localhost:5174

## 生产环境部署

### 方式一：使用部署脚本（推荐）

#### Linux/macOS
```bash
chmod +x deploy.sh
./deploy.sh
```

#### Windows
```bash
deploy.bat
```

### 方式二：手动部署

#### 1. 构建项目

```bash
# 后端
cd payment-system
mvn clean package -DskipTests

# 商家管理后台
cd payment-frontend-admin
npm install
npm run build

# 用户端应用
cd payment-frontend-user
npm install
npm run build
```

#### 2. 部署前端到 Nginx

```bash
# Linux
sudo cp -r payment-frontend-admin/dist/* /usr/share/nginx/html/admin/
sudo cp -r payment-frontend-user/dist/* /usr/share/nginx/html/user/
sudo cp nginx.conf /etc/nginx/nginx.conf
sudo nginx -s reload

# Windows
xcopy /E /I payment-frontend-admin\dist\* C:\nginx\html\admin\
xcopy /E /I payment-frontend-user\dist\* C:\nginx\html\user\
copy nginx.conf C:\nginx\conf\nginx.conf
cd C:\nginx
nginx -s reload
```

#### 3. 启动后端服务

```bash
cd payment-system
java -jar target/payment-system-1.0.0.jar
```

或使用 nohup 后台运行：
```bash
nohup java -jar target/payment-system-1.0.0.jar > app.log 2>&1 &
```

## 访问地址

### 开发环境
- 后端 API: http://localhost:8080/api
- 商家管理后台: http://localhost:5173
- 用户端应用: http://localhost:5174

### 生产环境（通过 Nginx）
- 商家管理后台: http://localhost 或 http://admin.payment.local
- 用户端应用: http://user.payment.local
- 后端 API: http://localhost/api (通过 Nginx 代理)

## 配置 hosts（可选）

编辑 hosts 文件：
- Windows: `C:\Windows\System32\drivers\etc\hosts`
- Linux/macOS: `/etc/hosts`

添加：
```
127.0.0.1  admin.payment.local
127.0.0.1  user.payment.local
```

## 环境变量说明

### 前端环境变量

#### 开发环境 (`.env.development`)
- 直连后端服务
- API 地址: `http://localhost:8080/api`

#### 生产环境 (`.env.production`)
- 通过 Nginx 代理
- API 地址: `/api` (相对路径)

### 后端配置 (`application.yml`)

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_system
    username: root
    password: your_password
  
  redis:
    host: localhost
    port: 6379
```

## 常见问题

### 1. 前端请求后端 404

**原因**: API 地址配置错误

**解决**:
- 开发环境: 检查 `.env.development` 中的 `VITE_API_BASE_URL`
- 生产环境: 检查 Nginx 配置中的 `proxy_pass`

### 2. 跨域问题

**原因**: 开发环境直连后端时可能出现跨域

**解决**: 
- 方式一: 后端添加 CORS 配置
- 方式二: 使用 Vite 代理（推荐开发环境）

在 `vite.config.js` 中添加：
```javascript
export default {
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
}
```

### 3. Nginx 启动失败

**检查配置**:
```bash
nginx -t
```

**查看日志**:
```bash
# Linux
tail -f /var/log/nginx/error.log

# Windows
type C:\nginx\logs\error.log
```

### 4. 后端连接数据库失败

**检查**:
- MySQL 是否启动
- 数据库是否创建
- 用户名密码是否正确
- 防火墙是否开放 3306 端口

## 性能优化建议

### 1. 前端优化
- 启用 Gzip 压缩（已在 Nginx 配置中）
- 静态资源 CDN 加速
- 图片懒加载
- 路由懒加载

### 2. 后端优化
- 数据库连接池配置
- Redis 缓存热点数据
- 接口响应压缩
- 异步处理耗时操作

### 3. Nginx 优化
- 启用 HTTP/2
- 配置缓存策略
- 负载均衡（多实例）
- 限流防护

## 监控和日志

### 应用日志
```bash
# 后端日志
tail -f payment-system/logs/app.log

# Nginx 访问日志
tail -f /var/log/nginx/access.log

# Nginx 错误日志
tail -f /var/log/nginx/error.log
```

### 健康检查
```bash
# 后端健康检查
curl http://localhost:8080/actuator/health

# Nginx 健康检查
curl http://localhost:8888/health
```

## 技术支持

如有问题，请查看：
1. `NGINX部署说明.md` - 详细的 Nginx 配置说明
2. `POS收银功能说明.md` - POS 功能使用说明
3. 项目 README 文件
