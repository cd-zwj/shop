# 双钱包一期已完成实现说明

## 1. 本次已完成范围

本次已完成的是一期闭环中的后端主干能力，重点围绕以下部分展开：

- 平台统一用户模型
- 双钱包模型
  - 统一钱包
  - 商户钱包
- 消费订单新链路
- 统一支付单模型
- 第三方支付占位抽象
- 充值业务新链路
- 支付回调统一入口
- 异步到账与异步结算
- 商户积分账户与积分发放
- 多租户忽略表配置调整
- v1 新接口落地
- 编译与测试验证

## 2. 已完成的数据库设计落地

已新增 SQL 文件：

- `payment-system/sql/14_platform_wallet_v1.sql`

已新增或落地的核心表：

- `platform_user`
- `platform_user_auth`
- `tenant_employee`
- `tenant_member`
- `unified_wallet_account`
- `unified_wallet_log`
- `merchant_wallet_account`
- `merchant_wallet_log`
- `sales_order`
- `sales_order_item`
- `merchant_recharge_rule`
- `recharge_order_v1`
- `payment_bill`
- `payment_callback_record`
- `member_points_account`
- `member_points_log`
- `message_outbox`
- `compensation_task`
- `dead_letter_task`

同时已更新：

- `payment-system/sql/import_all.sql`

## 3. 已完成的代码模块

### 3.1 枚举与基础工具

已新增：

- `WalletStrategyEnum`
- `WalletTypeEnum`
- `OrderStatusEnum`
- `PayStatusEnum`
- `PaymentBizTypeEnum`
- `PaymentChannelCodeEnum`
- `CallbackStatusEnum`
- `OutboxSendStatusEnum`
- `MessageProcessStatusEnum`
- `BizNoGenerator`
- `PlatformSessionHelper`

作用：

- 统一状态值和业务枚举
- 统一业务单号生成
- 统一获取平台用户会话

### 3.2 新实体与 Mapper

已为新增表补齐对应实体与 Mapper，覆盖：

- 平台用户
- 商户员工/会员关系
- 双钱包账户与流水
- 订单与支付单
- 充值单
- 积分账户与积分流水
- outbox / 补偿 / 死信任务

### 3.3 服务层

已完成的核心服务：

- `PlatformIdentityServiceImpl`
  - 平台用户注册
  - 平台用户登录
  - 获取当前平台用户

- `UnifiedWalletServiceImpl`
  - 统一钱包查询
  - 统一钱包流水查询
  - 统一钱包入账
  - 统一钱包扣减

- `MerchantWalletServiceImpl`
  - 商户钱包查询
  - 商户钱包流水查询
  - 商户钱包入账
  - 商户钱包扣减

- `MemberPointsAccountServiceImpl`
  - 商户积分账户查询
  - 商户积分流水查询
  - 商户积分发放

- `MerchantRechargeRuleServiceImpl`
  - 商户充值规则查询

- `ExtPaymentProviderImpl`
  - 第三方支付占位实现
  - 当前返回占位支付链接

- `PaymentBillV1ServiceImpl`
  - 创建统一支付单
  - 创建外部支付请求
  - 统一处理支付回调
  - 回调验签、落库、幂等、投递 MQ

- `AppOrderServiceImpl`
  - 用户端创建订单
  - 钱包策略拆分扣款
  - 纯钱包支付直接结算
  - 外部支付部分生成支付单
  - 订单查询
  - 订单取消

- `WalletRechargeServiceImpl`
  - 创建统一钱包充值单
  - 创建商户钱包充值单
  - 充值成功后统一/商户钱包到账
  - 商户充值赠送积分
  - 商户充值增加商户财务余额

### 3.4 异步消费者

已新增：

- `PaymentV1Consumer`

已实现：

- 充值成功后的异步到账
- 订单支付成功后的异步结算
- 商户财务余额入账
- 消费赠送积分

## 4. 已完成的接口

### 4.1 用户端 v1 接口

已新增控制器：

- `V1AppAuthController`
- `V1AppUserController`
- `V1AppCatalogController`
- `V1AppOrderController`
- `V1AppWalletController`
- `V1AppPaymentBillController`

已完成接口范围：

- `/v1/app/auth/register`
- `/v1/app/auth/login/password`
- `/v1/app/auth/login/sms`
- `/v1/app/auth/login/third-party`
- `/v1/app/auth/logout`
- `/v1/app/users/me`
- `/v1/app/tenants`
- `/v1/app/tenants/{tenantId}`
- `/v1/app/tenants/{tenantId}/products`
- `/v1/app/products/{productId}`
- `/v1/app/orders`
- `/v1/app/orders/{orderNo}`
- `/v1/app/orders/{orderNo}/cancel`
- `/v1/app/wallets/unified`
- `/v1/app/wallets/unified/logs`
- `/v1/app/wallets/unified/recharges`
- `/v1/app/tenants/{tenantId}/wallet`
- `/v1/app/tenants/{tenantId}/wallet/logs`
- `/v1/app/tenants/{tenantId}/recharge-rules`
- `/v1/app/tenants/{tenantId}/wallet/recharges`
- `/v1/app/tenants/{tenantId}/points`
- `/v1/app/tenants/{tenantId}/points/logs`
- `/v1/app/payment-bills/{billNo}`

### 4.2 开放支付回调接口

已新增控制器：

- `V1OpenPaymentController`

已完成接口：

- `/v1/open/payments/callbacks/{channelCode}`
- `/v1/open/payments/callbacks/{channelCode}/recharge`
- `/v1/open/payments/callbacks/{channelCode}/order`

## 5. 已完成的关键业务规则

### 5.1 双钱包规则

已支持以下钱包策略：

- `NO_WALLET`
- `UNIFIED_ONLY`
- `MERCHANT_ONLY`
- `MERCHANT_THEN_UNIFIED`
- `UNIFIED_THEN_MERCHANT`
- `CUSTOM_SPLIT`

已实现逻辑：

- 用户下单时显式传钱包策略
- 统一钱包可跨商户消费
- 商户钱包只能在所属商户消费
- 允许纯钱包支付
- 允许钱包 + 外部支付组合支付
- `CUSTOM_SPLIT` 校验金额不超过订单金额

### 5.2 充值规则

已实现：

- 统一钱包充值不依赖商户规则
- 商户钱包充值依赖商户充值规则
- 商户钱包充值到账时支持赠送金额
- 商户钱包充值到账时支持赠送积分

### 5.3 回调规则

已实现：

- 回调统一入口
- 回调先验签
- 回调记录落库
- 回调幂等处理
- 回调成功后投递 MQ
- 到账逻辑不在回调线程执行

## 6. 已完成的配置调整

已调整：

- `MyBatisPlusConfig`
  - 对平台级表和非租户表加入忽略租户拦截

- `RabbitMQConfig`
  - 新增 v1 充值到账异步队列
  - 新增 v1 订单支付成功异步队列

- `SaTokenConfig`
  - 放开 v1 登录注册与开放回调白名单

## 7. 已完成的测试与验证

已完成：

- `mvn -q -DskipTests compile`
  - 通过

- `mvn -q test -Dtest=PaymentServiceImplPropertyTest`
  - 通过

- `mvn -q test`
  - 通过

同时修复了：

- `PaymentServiceImplPropertyTest` 在 jqwik 场景下的初始化问题

## 8. 当前仍未完成部分

本次没有继续扩展以下内容，仍属于后续工作：

- 真实第三方支付渠道接入
- 第三方回调签名细节实现
- 商户端 `/api/v1/merchant` 全量接口
- 管理端 `/api/v1/admin` 全量接口
- 优惠券体系
- 平台积分体系
- AI 业务表与 AI 接口
- 前端适配新 v1 接口
- 旧系统与新系统的数据迁移脚本

## 9. 本次主要文件

### SQL

- `payment-system/sql/14_platform_wallet_v1.sql`
- `payment-system/sql/import_all.sql`

### 主要服务

- `payment-system/src/main/java/com/payment/service/impl/PlatformIdentityServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/UnifiedWalletServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/MerchantWalletServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/MemberPointsAccountServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/MerchantRechargeRuleServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/PaymentBillV1ServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/AppOrderServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/WalletRechargeServiceImpl.java`

### 主要接口

- `payment-system/src/main/java/com/payment/controller/V1AppAuthController.java`
- `payment-system/src/main/java/com/payment/controller/V1AppUserController.java`
- `payment-system/src/main/java/com/payment/controller/V1AppCatalogController.java`
- `payment-system/src/main/java/com/payment/controller/V1AppOrderController.java`
- `payment-system/src/main/java/com/payment/controller/V1AppWalletController.java`
- `payment-system/src/main/java/com/payment/controller/V1AppPaymentBillController.java`
- `payment-system/src/main/java/com/payment/controller/V1OpenPaymentController.java`

## 10. 说明

本次实现采用的是“增量兼容”方式：

- 没有直接删除旧接口
- 没有强拆旧订单和旧余额体系
- 先新增 v1 结构与链路
- 方便后续逐步切换前端和逐步迁移数据

如果后续需要，我可以继续补两份文档：

- `接口对接说明-v1.md`
- `后续待办与迁移建议.md`
