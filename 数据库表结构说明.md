# 数据库表结构说明

## 数据库概览

数据库名称：`payment_db`  
字符集：`utf8mb4`  
排序规则：`utf8mb4_unicode_ci`

## 表分类

### 1. 核心业务表

#### 1.1 租户和用户
- `tenant` - 租户（商家）表
- `sys_user` - 用户表
- `wechat_user` - 微信用户表

#### 1.2 商品管理
- `product` - 商品表
- `product_stock` - 商品库存表

#### 1.3 订单管理
- `payment_order` - 订单表
- `order_item` - 订单商品明细表
- `payment_record` - 支付记录表
- `refund_record` - 退款记录表

### 2. 用户资产系统

#### 2.1 余额系统
- `user_balance` - 用户余额表
- `balance_log` - 余额变动日志表

#### 2.2 积分系统
- `user_points` - 用户积分表
- `points_log` - 积分变动日志表
- `points_rule` - 积分规则表

#### 2.3 充值系统
- `recharge_rule` - 充值规则表
- `recharge_order` - 充值订单表

#### 2.4 积分兑换
- `exchange_product` - 积分兑换商品表
- `exchange_record` - 积分兑换记录表

### 3. 商家财务系统

- `merchant_balance` - 商家余额表
- `withdrawal` - 提现申请表

### 4. POS 收银系统

- `pos_session` - POS 会话表
- `scan_record` - 扫码记录表

### 5. 数据分析系统

- `sales_statistics_daily` - 销售统计表（按天）
- `product_sales_statistics` - 商品销售统计表
- `user_behavior_log` - 用户行为日志表
- `data_analysis_result` - 数据分析结果表

### 6. 系统配置

- `sys_config` - 系统配置表

## 详细表结构

### 租户表 (tenant)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_code | VARCHAR(32) | 租户编码（唯一） |
| name | VARCHAR(100) | 租户名称 |
| contact | VARCHAR(50) | 联系人 |
| phone | VARCHAR(20) | 联系电话 |
| address | VARCHAR(255) | 地址 |
| status | TINYINT | 状态：0-禁用，1-启用 |
| deleted | TINYINT | 是否删除：0-否，1-是 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_code`
- KEY: `status`

### 用户表 (sys_user)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| username | VARCHAR(50) | 用户名 |
| password | VARCHAR(255) | 密码（加密） |
| nickname | VARCHAR(50) | 昵称 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| avatar | VARCHAR(255) | 头像 |
| user_type | TINYINT | 用户类型：1-普通用户，2-管理员 |
| status | TINYINT | 状态：0-禁用，1-启用 |
| deleted | TINYINT | 是否删除：0-否，1-是 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_id, username`
- KEY: `phone`, `user_type`, `tenant_id`

### 商品表 (product)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| product_code | VARCHAR(64) | 商品编码（条码） |
| name | VARCHAR(200) | 商品名称 |
| price | DECIMAL(10,2) | 单价 |
| unit | VARCHAR(20) | 单位 |
| category | VARCHAR(50) | 分类 |
| image_url | VARCHAR(500) | 商品图片URL |
| description | TEXT | 商品描述 |
| status | TINYINT | 状态：0-下架，1-上架 |
| deleted | TINYINT | 是否删除：0-否，1-是 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_id, product_code`
- KEY: `tenant_id`, `product_code`, `status`

### 用户余额表 (user_balance)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| user_id | BIGINT | 用户ID |
| balance | DECIMAL(10,2) | 可用余额 |
| frozen_balance | DECIMAL(10,2) | 冻结余额 |
| total_recharge | DECIMAL(10,2) | 累计充值 |
| total_consume | DECIMAL(10,2) | 累计消费 |
| version | INT | 版本号（乐观锁） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_id, user_id`
- KEY: `user_id`

### 用户积分表 (user_points)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| user_id | BIGINT | 用户ID |
| points | INT | 当前积分 |
| total_earned | INT | 累计获得积分 |
| total_used | INT | 累计使用积分 |
| level | INT | 会员等级 |
| version | INT | 版本号（乐观锁） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_id, user_id`
- KEY: `user_id`, `level`

### 充值规则表 (recharge_rule)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| recharge_amount | DECIMAL(10,2) | 充值金额 |
| gift_amount | DECIMAL(10,2) | 赠送金额 |
| gift_points | INT | 赠送积分 |
| status | TINYINT | 状态：0-禁用，1-启用 |
| sort_order | INT | 排序 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 积分兑换商品表 (exchange_product)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| product_name | VARCHAR(200) | 商品名称 |
| product_image | VARCHAR(500) | 商品图片 |
| points_required | INT | 所需积分 |
| stock | INT | 库存数量 |
| exchange_limit | INT | 兑换限制（每人） |
| description | TEXT | 商品描述 |
| status | TINYINT | 状态：0-下架，1-上架 |
| sort_order | INT | 排序 |
| deleted | TINYINT | 是否删除：0-否，1-是 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 商家余额表 (merchant_balance)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| balance | DECIMAL(10,2) | 可用余额 |
| frozen_balance | DECIMAL(10,2) | 冻结余额 |
| total_income | DECIMAL(10,2) | 累计收入 |
| total_withdrawal | DECIMAL(10,2) | 累计提现 |
| version | INT | 版本号（乐观锁） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引：**
- PRIMARY KEY: `id`
- UNIQUE KEY: `tenant_id`

### 提现申请表 (withdrawal)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| withdrawal_no | VARCHAR(64) | 提现单号 |
| amount | DECIMAL(10,2) | 提现金额 |
| fee | DECIMAL(10,2) | 手续费 |
| actual_amount | DECIMAL(10,2) | 实际到账金额 |
| bank_name | VARCHAR(100) | 银行名称 |
| bank_account | VARCHAR(50) | 银行账号 |
| account_name | VARCHAR(100) | 账户名称 |
| status | VARCHAR(20) | 状态：PENDING-待审核，APPROVED-已通过，REJECTED-已拒绝，COMPLETED-已完成 |
| reject_reason | VARCHAR(500) | 拒绝原因 |
| remark | VARCHAR(500) | 备注 |
| apply_time | DATETIME | 申请时间 |
| audit_time | DATETIME | 审核时间 |
| complete_time | DATETIME | 完成时间 |
| deleted | TINYINT | 是否删除：0-否，1-是 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### POS 会话表 (pos_session)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键ID |
| tenant_id | BIGINT | 租户ID |
| session_id | VARCHAR(64) | 会话ID |
| device_id | VARCHAR(64) | 设备ID |
| cashier_id | BIGINT | 收银员ID |
| status | VARCHAR(20) | 状态：ACTIVE-活跃，CLOSED-已关闭 |
| cart_data | JSON | 购物车数据 |
| total_amount | DECIMAL(10,2) | 总金额 |
| expire_time | DATETIME | 过期时间 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

## 数据隔离策略

所有业务表都包含 `tenant_id` 字段，实现多租户数据隔离：

1. **查询时**：WHERE 条件必须包含 `tenant_id`
2. **插入时**：必须指定 `tenant_id`
3. **更新时**：WHERE 条件必须包含 `tenant_id`
4. **删除时**：使用软删除（`deleted = 1`），WHERE 条件必须包含 `tenant_id`

## 乐观锁机制

以下表使用 `version` 字段实现乐观锁，防止并发更新问题：

- `user_balance` - 用户余额
- `user_points` - 用户积分
- `merchant_balance` - 商家余额
- `product_stock` - 商品库存

**更新示例：**
```sql
UPDATE user_balance 
SET balance = balance + 100, version = version + 1 
WHERE user_id = 1 AND version = 0;
```

## 软删除

以下表使用软删除机制（`deleted` 字段）：

- `tenant` - 租户表
- `sys_user` - 用户表
- `product` - 商品表
- `payment_order` - 订单表
- `exchange_product` - 积分兑换商品表
- `withdrawal` - 提现申请表

**查询时需要过滤：**
```sql
SELECT * FROM product WHERE deleted = 0;
```

## 索引策略

### 1. 主键索引
所有表都有自增主键 `id`

### 2. 唯一索引
- 租户编码：`tenant_code`
- 订单号：`order_no`
- 商品编码：`tenant_id + product_code`
- 用户名：`tenant_id + username`

### 3. 普通索引
- 租户ID：`tenant_id`（几乎所有表）
- 状态字段：`status`
- 时间字段：`create_time`
- 外键字段：`user_id`, `product_id`, `order_id`

## 数据类型说明

### 金额字段
使用 `DECIMAL(10,2)` 存储金额，精确到分

### 时间字段
- `create_time`：创建时间，默认 `CURRENT_TIMESTAMP`
- `update_time`：更新时间，自动更新 `ON UPDATE CURRENT_TIMESTAMP`

### 状态字段
- 使用 `TINYINT` 或 `VARCHAR` 存储
- 建议使用枚举值或常量定义

### JSON 字段
- `product_snapshot`：商品快照
- `cart_data`：购物车数据
- `behavior_data`：行为数据

## 初始数据

### 默认租户
```sql
tenant_code: TENANT_001
name: 默认商户
```

### 默认管理员
```sql
username: admin
password: admin123 (加密后)
```

### 默认充值规则
- 100元 → 赠送0元 + 100积分
- 200元 → 赠送20元 + 250积分
- 500元 → 赠送60元 + 700积分
- 1000元 → 赠送150元 + 1500积分

### 默认积分规则
- 支付获得积分：每消费1元获得1积分
- 每日签到：10积分
- 分享好友：20积分

### 默认兑换商品
- 10元优惠券：1000积分
- 20元优惠券：2000积分
- 精美礼品：5000积分

## 性能优化建议

### 1. 分区表
对于大数据量表，建议按 `tenant_id` 或时间分区：
```sql
PARTITION BY HASH(tenant_id) PARTITIONS 10;
```

### 2. 读写分离
- 主库：写操作
- 从库：读操作、统计查询

### 3. 缓存策略
- 商品信息：Redis 缓存
- 用户余额：Redis 缓存
- 购物车：Redis 存储

### 4. 归档策略
定期归档历史数据：
- 订单数据：保留1年
- 日志数据：保留3个月
- 统计数据：永久保留

## 备份策略

### 1. 全量备份
每天凌晨进行全量备份

### 2. 增量备份
每小时进行增量备份（binlog）

### 3. 重要表实时备份
- `payment_order` - 订单表
- `payment_record` - 支付记录表
- `user_balance` - 用户余额表
- `merchant_balance` - 商家余额表
