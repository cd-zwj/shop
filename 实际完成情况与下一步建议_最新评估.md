# 项目完成度评估（基于《项目方向与需求规划.md》）

结合《项目方向与需求规划.md》以及当前代码库（特别是 `v1-api-boundary.md` 的范围定义），以下是对多租户系统当前开发进度的盘点，以及哪些部分尚未完成的详细分析。

## ✅ 已完成 / 具备雏形的部分

1. **基础架构与多租户隔离**
   - **多端分离**：后端接口已按照规范完成了 `/v1/app/`、`/v1/merchant/`、`/v1/admin/` 的命名空间分离。
   - **租户隔离**：通过 `TenantContextHolder` 在请求链中获取 `tenantId`，并且业务代码（如 `PointsController`）已经能根据租户去隔离数据。
   - **认证授权框架**：引入了 Sa-Token 进行会话管理及角色权限（RBAC）校验。

2. **核心业务闭环（基础版）**
   - **商品与订单**：提供了商品上下架管理（`ProductController` 等）及基础的销售订单创建（`SalesOrder`、`V1AppOrderController`）。
   - **充值与钱包**：支持钱包余额查询以及充值订单生成（`V1AppWalletController`、`RechargeController`）。
   - **积分体系**：积分规则、用户积分明细查询及积分兑换商品的基础链路已打通（`PointsController`）。

3. **消息中间件基础（RabbitMQ）**
   - 预留了 `OrderConsumer`、`PaymentV1Consumer`、`DeadLetterConsumer`，用于处理部分异步逻辑（如订单超时取消等）。

---

## ❌ 未完成 / 待完善的部分

### 1. 第三方支付接入仍需修复（高优先级）
目前您正在修改 `ExtPaymentProviderImpl.java` 对接 `myzfw` 支付网关。然而代码中查询支付状态的 `queryPayment` 方法**尚未完成对接适配**：
- **当前代码**：依然使用的是 `GET` 请求 `/api.php?act=order`，并判断 `code == "1"` 为成功。
- **缺失改造**：需要根据新文档变更为 `POST /api/pay/query`，并加入完整的签名参数（`timestamp`, `sign`, `sign_type`），成功状态码应该判定为 `"0"`。

### 2. 登录与账号体系未完全实现
- **现状**：`V1AppAuthController` 中虽然暴露了 `/login/sms`（短信登录）和 `/login/third-party`（第三方登录）的端点。
- **问题**：底层 `PlatformIdentityServiceImpl.java` 中，只有基于**账号密码**的注册和登录是真实逻辑。短信验证码服务、微信/GitHub 第三方授权登录的真实校验逻辑全部缺失。

### 3. 优惠与营销体系缺失（Coupon）
- **现状**：积分系统（Points）虽已上线，但**优惠券体系完全空白**。
- **缺失**：代码库中没有 `CouponController`、`Coupon` 实体或表。满减券、折扣券、充值赠券的发放、核销和组合抵扣逻辑均未开始开发。

### 4. 退款与支付兜底补偿链路未闭环
- **现状**：统一下单模型已经建好（`PaymentBill`），且有 RabbitMQ 消费者，但异常补偿不够完善。
- **缺失**：未与第三方支付真正打通**退款 API**（`Refund` 相关逻辑尚未在 `ExtPaymentProviderImpl` 中实现）。另外，如果第三方支付不回调，基于 RabbitMQ 延迟队列的主动状态补查尚未完全串联到外部供应商。

### 5. AI 助手深度能力未落地
- **现状**：系统中存在一个基础的 `AiController.java` 和 `DataAnalysisController.java`。
- **缺失**：距离规划文档中的“商家端智能客服对话”、“报表自动总结”、“用户个性化推荐”差距较大，目前仅为简单占位（受限于第一阶段目标，可暂缓）。

### 6. 财务报表与对账
- **现状**：只有基础的 `SalesStatisticsController.java`。
- **缺失**：跨商户平台的对账能力、租户资金结算流水、以及复杂的财务审计日志。

---

## 🚀 下一步开发建议

由于您在 `v1-api-boundary.md` 中约定了**“本轮只覆盖最小可管理闭环，不追求一次性铺满”**，建议按照以下顺序推进：

1. **彻底跑通外部支付网关**：先在 `ExtPaymentProviderImpl.java` 中完成 `/api/pay/query` 的 POST 和签名改造，确保一个完整的充值、消费、查询闭环可用。
2. **补齐真实认证逻辑**：完善短信验证码发送防刷机制以及至少一种第三方登录（如微信）的真实对接，替换现有的占位方法。
3. **完善退款与幂等补偿**：利用现有的 RabbitMQ 消费者，把超时未支付订单关闭、第三方支付状态主动反查、退款原路退回逻辑补充完整。
4. **（后期）开始建设优惠券中心**：待核心交易稳定后，再开辟表结构去写 `Coupon` 模块。
