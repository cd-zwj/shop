# 前后端对接 & 安全审计 -- 进度记录

> 日期：2026-06-04
> 分支：codex-dual-wallet-v1
> 团队：1架构师 + 2后端 + 1前端 + 1测试 + 1安全专家

---

## 一、架构师结论

**建议下一步：前端接口对接（阶段一公共底座 + 阶段二用户端基础链路）**

理由：后端 V1 API 37个Controller 已就绪，但前端全部是 Mock 数据，系统对用户不可用。最大瓶颈是前后端没有联调。

---

## 二、后端1号：接口返回结构审计

### 已修复 ✅

| 问题 | 严重性 | 修复方式 |
|------|--------|---------|
| LocalDateTime 未配置 ISO 8601 格式 | HIGH | 新增 `JacksonConfig.java`，全局配置 `yyyy-MM-dd'T'HH:mm:ss` |
| 分页返回结构与前端不一致 | HIGH | 新增 `PageResult.java`（records/total/page/size），10个 Controller 已修改 |

### 待处理 ⏳

| 问题 | 严重性 | 说明 |
|------|--------|------|
| Entity 直接暴露给前端 | HIGH | 缺少 VO 转换层，暴露 deleted/platformUserId 等内部字段 |
| 金额字段类型不一致 | CRITICAL | 后端用 BigDecimal，前端期望 Long（分）。建议 VO 层做转换 |
| Catalog 接口缺少分页 | MEDIUM | `/v1/app/tenants` 和商品列表无分页参数 |
| repayOrder 接口未记录在文档 | MEDIUM | `POST /v1/app/orders/{orderNo}/repay` 需补充文档 |
| AppCreateOrderDTO 与文档不一致 | MEDIUM | items[].price 字段需确认由谁传入 |
| 订单列表直接返回 SalesOrder | MEDIUM | 应创建 SalesOrderListVO |

### 新增文件

- `payment-system/src/main/java/com/payment/config/JacksonConfig.java`
- `payment-system/src/main/java/com/payment/common/PageResult.java`

### 修改文件（10个 Controller）

- V1AppOrderController.java
- V1AppWalletController.java
- V1AdminMerchantController.java
- V1AdminTradeController.java
- V1AdminUserController.java
- V1AdminWithdrawalController.java
- V1AdminAuthProviderController.java
- V1MerchantOrderController.java
- V1MerchantProductController.java
- V1MerchantWithdrawalController.java

---

## 三、后端2号：CORS + 接口一致性审计

### CRITICAL：CORS 配置完全缺失 ❌

WebConfig.java 是空壳，没有任何 CORS 配置。前端 localhost:5173 的所有跨域请求会被浏览器拦截。

**修复方案已提供**：使用 `CorsFilter`（Filter 级别），而非 `addCorsMappings`（Interceptor 级别），确保 OPTIONS 预检请求在 Sa-Token 拦截器之前通过。

**修复代码**：见下方"待执行修复"章节。

### Sa-Token 配置确认 ✅

- Token 通过 Header（Authorization）传递，适合 SPA
- 白名单路径与前端文档一致
- 无需修改

### 路径前缀一致性 ✅

23个 V1 Controller 路径前缀全部一致：`/v1/app/*`、`/v1/merchant/*`、`/v1/admin/*`、`/v1/open/*`

### 请求参数命名 ✅

全部 camelCase，与前端文档一致。

### 发现的其他问题

| 问题 | 严重性 |
|------|--------|
| Entity 直接暴露在响应中 | HIGH |
| 查询 null 返回 200 而非 404 | HIGH |
| Map 接收请求体缺 DTO | MEDIUM |
| @Valid 注解缺失 | MEDIUM |
| Controller 直接注入 Mapper | MEDIUM |
| application-dev.yml 硬编码密钥 | CRITICAL |
| 错误码定义不完整 | HIGH |

---

## 四、前端：公共底座搭建

### 现状：大部分底座已存在 ✅

经审查，公共底座核心组件**已经实现良好**：
- `services/http.ts` -- axios 实例 + 请求拦截器 + token 注入
- `services/request.ts` -- 统一 request 封装，code !== 200 统一 reject
- `utils/authSession.ts` -- localStorage 会话持久化
- `types/api.ts` -- ApiResponse<T>、PageResult<T>
- `types/auth.ts` -- AuthRole、PlatformUser、PlatformLoginDTO 等
- `types/merchant.ts` -- 商户/商品全套类型
- `services/modules/appAuth.ts` -- 已对接真实 `/v1/app/auth/*` 接口
- `config/env.ts` -- 从环境变量读取 baseURL

### 本次新增/修改

| 操作 | 文件 | 内容 |
|------|------|------|
| 新增 | `types/user.ts` | UserProfile、UpdateUserProfilePayload、ChangePasswordPayload 等 |
| 新增 | `types/index.ts` | 类型统一 barrel 导出 |
| 修改 | `utils/token.ts` | 存储 key 统一为 salessystem:app/merchant/platform:token |
| 修改 | `services/http.ts` | **关键**：401 响应时 dispatch CustomEvent 清除 token + 跳转登录 |
| 修改 | `context/AuthContext.tsx` | 监听 401 事件、提取 resetLocalAuthState()、统一清理逻辑 |

### 架构设计决策

1. **401 处理采用事件机制**：http.ts 通过 CustomEvent 松耦合通信，避免循环依赖
2. **token 存储 key 加 salessystem: 前缀**：避免 localStorage 冲突
3. **类型检查通过**：`npx tsc --noEmit` 无错误

### 数据流

```
Login -> AuthContext.loginUser()
  -> appAuthService.loginByPassword() // POST /v1/app/auth/login/password
  -> setToken('user', token)          // localStorage
  -> appUserService.getCurrentUser()  // GET /v1/app/users/me
  -> setCurrentUser()

401 -> dispatch AUTH_TOKEN_CLEAR_EVENT -> resetLocalAuthState -> /login
```

---

## 五、安全专家：全面安全审计

### CRITICAL（5项）

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 1 | 注册提权漏洞 | UserController.java:41 | 接受 User 实体，攻击者可设 userType=2 注册管理员 |
| 2 | 硬编码敏感凭证 | application-dev.yml | JWT secret、AI API Key、数据库密码、MinIO 密钥 |
| 3 | 旧版登录无暴力破解防护 | UserController:28, AdminController:60 | 无验证码无限流 |
| 4 | AdminController 用 Map 接收参数 | AdminController:60 | 绕过 @Valid 校验 |
| 5 | 支付回调缺少签名验证 | PaymentController:27, V1OpenPaymentController:23 | 可伪造支付成功通知 |

### HIGH（6项）

| # | 问题 | 文件 |
|---|------|------|
| 6 | 支付账单查询水平越权 | V1AppPaymentBillController:22 |
| 7 | 返回 User 实体可能泄露密码哈希 | UserController:48, User.java:29 |
| 8 | 权限异常泄露内部权限码 | GlobalExceptionHandler:54 |
| 9 | 提现端点缺 @SaCheckLogin | V1MerchantWithdrawalController:28 |
| 10 | 订单查询水平越权 | MiniProgramController:121 |
| 11 | 缺少登录失败次数限制 | 所有登录 Controller |

### MEDIUM（9项）

| # | 问题 |
|---|------|
| 12 | Swagger UI 生产环境未关闭 |
| 13 | Sa-Token active-timeout=-1 无活跃超时 |
| 14 | 允许同一账号并发登录 |
| 15 | 验证码 IP 依赖 X-Forwarded-For（可伪造） |
| 16 | MerchantController 缺细粒度权限 |
| 17 | 提现审核余额扣减缺分布式锁 |
| 18 | 新增用户余额缺并发保护 |
| 19 | UUID token 不如 JWT |
| 20 | .last("limit 1") 潜在 SQL 注入风险 |

### LOW（4项）

| # | 问题 |
|---|------|
| 21 | 缺少全局 CORS 配置 |
| 22 | 数据库 useSSL=false |
| 23 | SQL 日志 StdOutImpl |
| 24 | Redis 无密码保护 |

### 最紧急 Top 3

1. **注册提权漏洞** -- 可立即利用，任何人可注册管理员
2. **支付回调签名验证缺失** -- 可伪造支付成功，直接资金损失
3. **旧版登录无暴力破解防护** -- 可无限尝试密码

---

## 六、测试工程师

状态：报告未返回（agent 运行时间较长），待后续补充。

---

## 七、待执行修复（代码已准备好，待手动应用）

### 修复1：CORS 配置

文件：`payment-system/src/main/java/com/payment/config/WebConfig.java`

```java
package com.payment.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
import org.springframework.web.filter.CorsFilter;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.addAllowedOrigin("http://localhost:5173");
        config.addAllowedOrigin("http://localhost:3000");
        config.setAllowCredentials(true);
        config.addAllowedHeader("*");
        config.addAllowedMethod("*");
        config.addExposedHeader("Authorization");
        config.addExposedHeader("Content-Disposition");
        config.setMaxAge(3600L);

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        return new CorsFilter(source);
    }
}
```

### 修复2：注册接口提权防护

需要创建 RegisterDTO，只含 username/password/nickname/phone，Service 层强制 userType=1。

### 修复3：支付回调签名验证

- 微信：使用 NotificationParser 验证签名
- 支付宝：使用 AlipaySignature.rsaCheckV1()
- 增加幂等校验

---

## 八、下一步行动建议

### 优先级 P0（阻塞前后端联调）

1. ~~应用 CORS 配置~~ （代码已准备好，待应用）
2. 处理金额类型不一致（VO 层 BigDecimal -> Long 转换）
3. Entity 暴露问题（创建 VO 类）

### 优先级 P1（安全加固）

1. 注册提权漏洞修复
2. 支付回调签名验证
3. 旧版接口废弃或加固
4. 水平越权校验

### 优先级 P2（继续前端对接）

1. 页面层对接（Login/Register/Home/Discovery/ProductDetails/Profile）
2. 商户端/管理端对接
3. 补充营销/优惠券前端页面

### 优先级 P3（完善）

1. 错误码体系统一补全
2. 接口文档补充
3. 测试覆盖率提升
