# 支付宝充值 & 退款完成情况评估

> 评估时间：2026-05-04
> 评估范围：`payment-system` 模块中支付宝充值、回调、退款相关代码

---

## 一、已完成 / 做得不错的部分

### 1. 支付宝电脑网站支付 (`AlipayPagePaymentProvider`)

| 功能 | 状态 | 说明 |
|---|---|---|
| 下单 `createPayment` | ✅ 完成 | 使用官方 SDK `AlipayTradePagePayRequest`，金额精度正确 |
| 回调验签 `verifyCallback` | ✅ 完成 | 使用 `AlipaySignature.rsaCheckV1` RSA2 验签，安全可靠 |
| 订单查询 `queryPayment` | ✅ 完成 | 使用 `AlipayTradeQueryRequest`，`TRADE_SUCCESS`/`TRADE_FINISHED` 状态映射正确 |
| 异步通知接口 | ✅ 完成 | `POST /v1/open/payments/callbacks/alipay-page` |
| 同步跳转返回页 | ✅ 完成 | `GET /v1/open/payments/returns/alipay-page`，返回友好 HTML 提示页 |
| 幂等性（重复回调保护） | ✅ 完成 | 通过 `callbackRequestId` 去重，防止重复到账 |

### 2. 充值业务流程 (`WalletRechargeService`)

| 功能 | 状态 | 说明 |
|---|---|---|
| 统一钱包充值下单 | ✅ 完成 | `createUnifiedRecharge`，支持用户自由输入金额 |
| 商户钱包充值下单 | ✅ 完成 | `createMerchantRecharge`，按商户规则充值，支持赠送积分 |
| 充值成功到账 | ✅ 完成 | `handleRechargeSuccess` 正确调用 `credit` 方法更新钱包 |
| 赠送积分 | ✅ 完成 | 商户充值时同步调用 `memberPointsAccountService.grantPoints` |
| 商户财务余额同步 | ✅ 完成 | 调用 `withdrawalService.addMerchantBalance`（现金归商户） |
| 充值规则管理 | ✅ 完成 | `MerchantRechargeRuleService` 支持启用/禁用，前端可配置 |

### 3. 支付消息与可靠性架构

| 功能 | 状态 | 说明 |
|---|---|---|
| 支付成功后 MQ 事件 | ✅ 完成 | 区分充值/订单，分别路由到 `V1_RECHARGE_SUCCESS_QUEUE` 或 `V1_ORDER_PAID_QUEUE` |
| 消息发件箱（Outbox 模式） | ✅ 完成 | `MessageOutbox` 防止 MQ 消息丢失，先入库再发送 |
| 迟到回调补偿任务 | ✅ 完成 | `CompensationTask` 机制记录异常情况，支持人工/自动补偿 |
| 迟到回调策略分类 | ✅ 完成 | `PaymentStatusReasonEnum` 区分超时/取消/人工审核三种情形 |

---

## 二、缺失 / 风险较大的部分

### ❌ 1. 退款功能完全缺失（最高优先级）

这是整个支付体系最大的空白。

**根本原因**：`PaymentProvider` 接口只定义了三个方法：

```java
// 当前 PaymentProvider 接口
String getChannelCode();
PayResponseDTO createPayment(PaymentBill paymentBill);
boolean verifyCallback(PaymentCallbackDTO callbackDTO);
ExternalPaymentQueryResult queryPayment(PaymentBill paymentBill);
// ❌ 没有 refund() 方法！
```

**影响范围**：

- 支付宝 `AlipayTradeRefundRequest` **从未被调用过**
- `ExtPaymentProviderImpl`（myzfw 渠道）也没有退款接口调用
- 代码中的 `LATE_CALLBACK_REFUND` 补偿任务**只是"标记了需要退款"，并未真正发起退款**
- 订单取消时的"退款"仅是内部钱包余额回退（余额抵扣的部分），**对于直接支付宝付款的场景完全没有退款路径**

**需要实现的内容**：

```
1. PaymentProvider 接口 → 新增 refund(PaymentBill, BigDecimal) 方法
2. AlipayPagePaymentProvider → 实现 AlipayTradeRefundRequest
3. ExtPaymentProviderImpl → 对接 myzfw 退款 API
4. 补偿任务消费者 → 处理 LATE_CALLBACK_REFUND 类型任务，调用真实退款
5. 退款记录表（可选）→ 记录退款流水，便于对账
```

---

### ⚠️ 2. 旧版 `RechargeController` 与 V1 流程并存（中等风险）

- **旧版**：`RechargeController`（路径 `/api/recharge`），使用旧 `PaymentOrder` 实体，硬编码 `payType = "WECHAT"`
- **新版**：`V1AppWalletController`（路径 `/v1/app`）+ `WalletRechargeService`，架构更完善
- **问题**：两套并存，前端如果误用旧接口，会走到残缺的支付逻辑

> **建议**：明确废弃旧版 `RechargeController`，或在接口上加 `@Deprecated` 注释并指向新版路径。

---

### ⚠️ 3. 旧版充值订单号生成有并发碰撞风险（低风险）

```java
// RechargeServiceImpl.java（旧版）
order.setOrderNo("R" + System.currentTimeMillis() + UUID.randomUUID().toString().substring(0, 8));
//                    ↑ 高并发下可能重复              ↑ 只取8位，碰撞概率上升
```

新版 `WalletRechargeService` 使用 `BizNoGenerator.generate("RC")` 更为可靠，旧版应统一替换。

---

### ⚠️ 4. 旧版异常类型不规范（低风险）

`RechargeServiceImpl` 中多处抛出裸 `RuntimeException`，未使用项目统一的 `BusinessException`，会导致全局异常处理器无法正确识别并返回标准错误响应。

```java
// 当前（需改造）
throw new RuntimeException("充值规则不存在");

// 应改为
throw new BusinessException("充值规则不存在");
```

---

## 三、建议优先级排序

| 优先级 | 任务 | 涉及文件 |
|---|---|---|
| 🔴 P0 | 在 `PaymentProvider` 接口新增 `refund()` 方法 | `PaymentProvider.java` |
| 🔴 P0 | `AlipayPagePaymentProvider` 实现 `AlipayTradeRefundRequest` | `AlipayPagePaymentProvider.java` |
| 🔴 P0 | `ExtPaymentProviderImpl` 对接 myzfw 退款 API | `ExtPaymentProviderImpl.java` |
| 🔴 P0 | 补偿任务消费者处理 `LATE_CALLBACK_REFUND`，调用真实退款 | 新增消费者类 |
| 🟡 P1 | 清理或废弃旧版 `RechargeController` | `RechargeController.java` |
| 🟡 P1 | 旧版 `RechargeServiceImpl` 异常替换为 `BusinessException` | `RechargeServiceImpl.java` |
| 🟢 P2 | 旧版订单号生成改用 `BizNoGenerator` | `RechargeServiceImpl.java` |

---

## 四、总结

> **充值下单和支付回调链路已经比较完整，架构设计（Outbox + 补偿任务 + MQ）也较为成熟。**
>
> **但退款功能是整个支付体系最大的空白**——当前只有"标记需要退款"的占位逻辑，没有任何真实退款 API 调用。
>
> 这是下一步最需要补全的核心功能，否则一旦出现支付异常或用户申请退款，系统将无法自动处理，只能完全依赖人工介入。
