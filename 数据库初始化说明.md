# 数据库初始化说明

## SQL 文件位置

```
payment-system/sql/
├── payment_db.sql              # 单租户版本
└── payment_db_multitenant.sql  # 多租户版本（推荐）
```

## 版本说明

### 多租户版本 (payment_db_multitenant.sql) - 推荐

适用于多商家电商系统，支持：
- 多个商家（租户）独立管理
- 数据隔离（通过 tenant_id）
- 商家管理功能
- 平台管理功能

**主要表结构：**
- `tenant` - 租户（商家）表
- `sys_user` - 用户表（带 tenant_id）
- `product` - 商品表（带 tenant_id）
- `payment_order` - 订单表（带 tenant_id）
- 其他业务表...

### 单租户版本 (payment_db.sql)

适用于单商家系统，不包含租户隔离功能。

## 初始化步骤

### 方式一：命令行导入（推荐）

#### Windows

```bash
# 进入 MySQL 安装目录的 bin 文件夹
cd C:\Program Files\MySQL\MySQL Server 8.0\bin

# 导入多租户版本
mysql -u root -p < C:\path\to\payment-system\sql\payment_db_multitenant.sql

# 输入密码后等待导入完成
```

#### Linux/macOS

```bash
# 导入多租户版本
mysql -u root -p < payment-system/sql/payment_db_multitenant.sql

# 或者指定数据库
mysql -u root -p payment_db < payment-system/sql/payment_db_multitenant.sql
```

### 方式二：MySQL Workbench

1. 打开 MySQL Workbench
2. 连接到数据库
3. 点击 `File` → `Run SQL Script`
4. 选择 `payment-system/sql/payment_db_multitenant.sql`
5. 点击 `Run` 执行

### 方式三：Navicat

1. 打开 Navicat
2. 连接到 MySQL 服务器
3. 右键点击连接 → `Execute SQL File`
4. 选择 `payment-system/sql/payment_db_multitenant.sql`
5. 点击 `Start` 执行

### 方式四：phpMyAdmin

1. 登录 phpMyAdmin
2. 点击 `Import` 标签
3. 选择文件：`payment_db_multitenant.sql`
4. 点击 `Go` 执行

## 验证安装

### 1. 检查数据库是否创建

```sql
SHOW DATABASES LIKE 'payment_db';
```

### 2. 检查表是否创建

```sql
USE payment_db;
SHOW TABLES;
```

应该看到以下表：
- tenant
- sys_user
- product
- payment_order
- user_balance
- balance_log
- user_points
- points_log
- recharge_rule
- recharge_order
- exchange_product
- withdrawal
- pos_session
- merchant_balance
- 等等...

### 3. 检查表结构

```sql
DESC tenant;
DESC product;
DESC payment_order;
```

## 初始数据

### 创建测试租户（商家）

```sql
INSERT INTO `tenant` (`tenant_code`, `name`, `contact`, `phone`, `address`, `status`) 
VALUES 
('MERCHANT001', '测试商家1', '张三', '13800138000', '北京市朝阳区', 1),
('MERCHANT002', '测试商家2', '李四', '13800138001', '上海市浦东新区', 1);
```

### 创建测试用户

```sql
-- 商家1的管理员（密码：123456，需要加密）
INSERT INTO `sys_user` (`tenant_id`, `username`, `password`, `nickname`, `user_type`, `status`) 
VALUES 
(1, 'admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVKIUi', '管理员', 2, 1);

-- 商家1的普通用户
INSERT INTO `sys_user` (`tenant_id`, `username`, `password`, `nickname`, `user_type`, `status`) 
VALUES 
(1, 'user001', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVKIUi', '用户001', 1, 1);
```

### 创建测试商品

```sql
INSERT INTO `product` (`tenant_id`, `product_code`, `name`, `price`, `stock`, `category`, `status`) 
VALUES 
(1, 'P001', '测试商品1', 99.00, 100, '电子产品', 1),
(1, 'P002', '测试商品2', 199.00, 50, '电子产品', 1),
(2, 'P003', '测试商品3', 299.00, 30, '家居用品', 1);
```

## 配置应用连接

### 修改 application.yml

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
```

### 修改 application.properties

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/payment_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=your_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
```

## 常见问题

### 1. 导入时报错：Unknown database 'payment_db'

**原因**: 数据库不存在

**解决**: SQL 脚本会自动创建数据库，确保使用完整的脚本文件

### 2. 导入时报错：Access denied

**原因**: 用户权限不足

**解决**: 
```sql
-- 授予权限
GRANT ALL PRIVILEGES ON payment_db.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
```

### 3. 字符集问题

**确保使用 UTF8MB4**:
```sql
ALTER DATABASE payment_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. 表已存在

**清空数据库重新导入**:
```sql
DROP DATABASE IF EXISTS payment_db;
-- 然后重新导入 SQL 文件
```

### 5. 连接超时

**修改 MySQL 配置** (my.ini 或 my.cnf):
```ini
[mysqld]
max_allowed_packet=64M
wait_timeout=28800
interactive_timeout=28800
```

## 数据库备份

### 备份整个数据库

```bash
mysqldump -u root -p payment_db > payment_db_backup_$(date +%Y%m%d).sql
```

### 备份指定表

```bash
mysqldump -u root -p payment_db tenant sys_user product > backup_tables.sql
```

### 恢复备份

```bash
mysql -u root -p payment_db < payment_db_backup_20241111.sql
```

## 数据库迁移

### 从单租户迁移到多租户

如果已经使用了单租户版本，需要迁移到多租户版本：

1. 备份现有数据
2. 创建默认租户
3. 为所有表添加 tenant_id 字段
4. 更新所有数据的 tenant_id

```sql
-- 1. 创建默认租户
INSERT INTO `tenant` (`tenant_code`, `name`, `status`) 
VALUES ('DEFAULT', '默认商家', 1);

-- 2. 获取租户ID（假设为1）
SET @default_tenant_id = 1;

-- 3. 更新现有数据
UPDATE `product` SET `tenant_id` = @default_tenant_id WHERE `tenant_id` IS NULL;
UPDATE `payment_order` SET `tenant_id` = @default_tenant_id WHERE `tenant_id` IS NULL;
-- ... 其他表
```

## 性能优化建议

### 1. 添加索引

```sql
-- 为常用查询字段添加索引
CREATE INDEX idx_product_tenant_status ON product(tenant_id, status);
CREATE INDEX idx_order_tenant_status ON payment_order(tenant_id, order_status);
CREATE INDEX idx_user_tenant_username ON sys_user(tenant_id, username);
```

### 2. 分区表（大数据量时）

```sql
-- 按租户ID分区
ALTER TABLE payment_order 
PARTITION BY HASH(tenant_id) 
PARTITIONS 10;
```

### 3. 定期清理

```sql
-- 清理已删除的数据（软删除）
DELETE FROM product WHERE deleted = 1 AND update_time < DATE_SUB(NOW(), INTERVAL 90 DAY);
```

## 监控和维护

### 查看表大小

```sql
SELECT 
    table_name AS '表名',
    ROUND(((data_length + index_length) / 1024 / 1024), 2) AS '大小(MB)'
FROM information_schema.TABLES 
WHERE table_schema = 'payment_db'
ORDER BY (data_length + index_length) DESC;
```

### 查看索引使用情况

```sql
SELECT * FROM sys.schema_unused_indexes WHERE object_schema = 'payment_db';
```

### 优化表

```sql
OPTIMIZE TABLE product;
OPTIMIZE TABLE payment_order;
```

## 技术支持

如有问题，请参考：
- MySQL 官方文档: https://dev.mysql.com/doc/
- 项目 README.md
- 快速启动指南.md
