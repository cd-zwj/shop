# 支付路径剩余待办清单

> 分支：codex-dual-wallet-v1
> 生成时间：2026-06-05
> 范围：支付路径相关，包括验签、回调、幂等、状态查询、登录前置路径与前后端配套收口

---

## 一、当前已基本完成

### 1. v1 支付主路径验签已建立
- 已建立 `PaymentSignatureVerifierImpl`，并在关键路径替代原桩逻辑。
- 支付宝异步通知验签、`app_id` 校验、`trade_status` 过滤已落地。
- 验签失败不再直接进入 `paymentBillV1Service.handleCallback`。

### 2. `PaymentSignatureVerifierStub` 已被控制
- 已在 team worktree 中改为：
  - fail-closed（不再返回 true）
  - `@Profile("dev")`
- 生产环境不再应静默注入该桩。

### 3. v1 支付单状态查询已加固
- `/v1/open/payments/bills/{billNo}/status` 已增加：
  - billNo 输入校验
  - 存在性预检查
  - 通用返回，降低枚举泄露风险

### 4. `PaymentV1Consumer` 已做原子化幂等处理
- 已改为条件更新，避免并发重复处理造成重复积分/余额/扣库存。

### 5. 前端支付结果链路已确认
- 前端未把浏览器同步返回当作真实支付成功。
- 真实支付状态以 API polling 为准。

### 6. 前端路由守卫已在 team worktree 中落地
- 已在隔离 worktree 中新增 admin / merchant / user route guard。
- 但需核对是否已落到主前端 `salessystem`。

---

## 二、当前必须继续完成的待办

### P0-1 旧支付路径未彻底关闭
当前仍存在的历史风险：

- 旧 `PaymentController`
- 旧 `PaymentServiceImpl`
- 旧 `PaymentOrderServiceImpl`
- 旧 `OrderConsumer`

建议动作：

1. 审查旧 `/payment/*` 回调接口是否仍在生产路径上
2. 若已不再使用，优先删除或默认禁用
3. 若仍临时使用，必须补齐与 v1 相同的验签与幂等能力

优先级：**P0**

---

### P0-2 CORS 必须配置化
当前配置仅允许：

- `http://localhost:5173`
- `http://localhost:3000`

影响：

- 生产环境前端域名非 localhost 时将直接失败
- 部署到测试/预发/生产环境前必须完成外部化

建议动作：

1. 将允许来源迁移到 `application.yml` / 环境变量
2. 按 profile 区分 dev / prod
3. 尽量避免 `*` + credentials

优先级：**P0**

---

### P0-3 `PaymentSignatureVerifierStub` 需彻底退役
当前状态已比之前安全，但仍建议：

1. 直接删除桩实现，或仅保留于文档/测试
2. 若保留，必须：
   - `@Profile("dev")`
   - fail-closed
   - 无生产 bean 注册
3. 最终以生产不可注入为准

优先级：**P0**

---

### P0-4 V1 登录接口限流需最终确认
涉及接口：

- `POST /v1/app/auth/login/password`
- `POST /v1/app/auth/login/sms`
- `POST /v1/app/auth/login/third-party`

当前风险：

- 若 v1 登录接口缺少限流，会影响支付前置认证安全

建议动作：

1. 确认是否已补限流注解或网关限流
2. 若未补，补齐账号+IP 限流

优先级：**P0**

---

### P1-1 回调与状态查询的 rate limit 仍需加强
当前 open 支付接口已验签，但：

- `/v1/open/payments/callbacks/**`
- `/v1/open/payments/bills/{billNo}/status`

建议动作：

1. 增加网关/应用层限流
2. 防止刷写 callback record 和日志资源消耗
3. 推荐状态查询按 IP + billNo 做限制

优先级：**P1**

---

### P1-2 `payment_callback_record` 唯一约束建议补齐
当前幂等更多依赖应用层查询：

- `channel_code + callback_request_id`

建议动作：

1. 增加数据库唯一约束或组合约束
2. 处理 `callbackRequestId` 为空时的幂等策略
3. 防止并发重复插入

优先级：**P1**

---

### P1-3 EXT_PROVIDER 回调路径仍需收口
当前问题：

- 旧路径存在 GET 回调形态
- sign 字段排除规则未完全确认
- provider 实现缺失或不稳定

建议动作：

1. 统一为 POST
2. 明确签名排除字段
3. 禁止用 query params 承载财务回调

优先级：**P1**

---

### P2-1 旧 MQ 与旧 Consumer 需清理
当前仍存在：

- 旧 `OrderConsumer`
- 旧 queue 相关配置
- 旧事件路径

建议动作：

1. 确认旧 queue 是否仍被使用
2. 若无生产用途，直接下线
3. 避免与 v1 consumer 混淆

优先级：**P2**

---

### P2-2 旧 `queryAlipayOrder` 假成功路径需清理
当前旧路径中仍有：

- 支付宝查询返回 TODO / 假状态

建议动作：

1. 若旧路径已废弃，删除该实现
2. 若保留，补齐真实查询逻辑或抛出未支持异常

优先级：**P2**

---

### P2-3 前端路由守卫需确认是否落到主前端
当前 team worktree 已在：

- `payment-frontend-admin/`
- `payment-frontend-user/`

但主前端仍疑似为：

- `salessystem/`

建议动作：

1. 核实这两个前端目录是否属于当前主项目
2. 若不是，迁移 route guard 逻辑到 `salessystem`
3. 保护 `/admin/**`、`/merchant/**`、用户私有页面

优先级：**P1**

---

### P2-4 `PageResult` 字段漂移应预留方案
当前后端为：

- `records / total / page / size`

当前前端为：

- `records / total / size / current / pages`

建议动作：

1. 暂可继续用 `records`
2. 后续做分页前统一字段
3. 推荐对齐到 `current` 或 `page` 二选一

优先级：**P2**

---

## 三、支付路径建议执行顺序

### 第一阶段：安全收口
1. 删除或彻底禁用旧支付路径
2. 删除/隔离 `PaymentSignatureVerifierStub`
3. 确认 V1 登录限流
4. 配置化 CORS

### 第二阶段：幂等与接口安全
5. 增加 `payment_callback_record` 唯一约束
6. 加强回调与状态查询限流
7. 收口 EXT_PROVIDER 回调

### 第三阶段：架构清理
8. 清理旧 MQ / 旧 Consumer
9. 清理旧假成功路径
10. 确认主前端并落地路由守卫

---

## 四、当前状态判断

### 已明显降低的风险
- v1 主路径不再使用无条件放行桩
- 支付宝异步通知验签更完整
- `PaymentV1Consumer` 幂等更安全
- 状态查询枚举风险下降

### 仍未彻底消除的风险
- 旧支付路径
- CORS 部署风险
- V1 登录限流确认
- 幂等数据库层保障
- 前端主仓库守卫落点确认

---

## 五、最终结论

当前支付路径已经从“整体存在严重安全缺口”推进到：

- **v1 主路径安全基本建立**
- **主要风险已转向旧路径、部署配置和收口不彻底**

当前最需要继续完成的三项：

1. **彻底关闭或禁用旧支付路径**
2. **CORS 外部化**
3. **确认主前端并完成路由守卫最终落地**
