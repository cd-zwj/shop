# 废弃接口本地保留说明

以下接口已标记为 `@Deprecated`，并仅用于本地、测试或迁移参考，不应作为新的生产功能入口继续扩展：

- `PosController`
- `DataAnalysisController`
- `SalesStatisticsController`

新功能应优先接入 `/v1/*` 命名空间下的 App、Merchant、Admin 控制器。旧接口如果仍被本地脚本或历史页面引用，应先完成迁移再删除。
