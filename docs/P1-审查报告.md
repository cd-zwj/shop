# P1 修复审查报告（第二轮）

## 审查范围

commit `2704b46` 声称修复 P1 全部 12 项问题。第一轮发现 3 项未修复、3 项部分修复。用户修改后第二轮复查。

---

## 终态总览

| 状态 | 数量 | 项目 |
|------|------|------|
| ✅ 完全修复 | 10 | #5, #6, #7, #8, #9.a, #9.b, #10, #11, #12(部分), #14, #15 |
| ⚠️ 部分修复 | 2 | #12(Coupon), #13, #16 |

---

## 逐项详情（仅列出二轮有变化的项）

### ✅ #8 安全响应头 — 已补全

`WebConfig.java` 已添加 HSTS + CSP：

```
Strict-Transport-Security: max-age=31536000; includeSubDomains
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self'
```

CSP 策略较为宽松（`unsafe-inline` 开放），对于内部管理系统可接受。

---

### ✅ #9.b PromotionServiceImpl N+1 — 已修复

`matchPromotions` 重构：

```java
// line 180-187: 批量预加载
Set<Long> activityIds = activities.stream()
    .map(PromotionActivity::getId)
    .collect(Collectors.toCollection(HashSet::new));
Map<Long, List<ActivityRule>> rulesMap = batchLoadRules(activityIds);

return activities.stream()
    .map(activity -> matchActivityWithRules(activity, 
        rulesMap.getOrDefault(activity.getId(), Collections.emptyList()), items))
```

`batchLoadRules`（line 295+）一次 IN 查询所有 ActivityRule 按 activityId 分组。旧 `matchActivity`（line 272）保留但已成死代码。

---

### ✅ #10 关键索引 — 已补全

`30_critical_indexes.sql` 新增第 4 条：

```sql
ALTER TABLE `member_points_log` ADD INDEX `idx_user_tenant_biz` 
  (`platform_user_id`, `tenant_id`, `biz_type`, `biz_no`, `status`);
```

覆盖索引设计合理：前导列 `platform_user_id` 匹配最频繁的查询模式。

---

### ⚠️ #12 多服务无分页 — DataAnalysis 修了，Coupon 未修

| 服务 | 接口 | 实现 | 状态 |
|------|------|------|------|
| DataAnalysisServiceImpl | `getAnalysisList(String, Integer, Integer) → Page<>` | `page()` 调用 | ✅ |
| ProductServiceImpl | `listProducts(Page, ...)` | 已有 | ✅ |
| CouponServiceImpl | — | 无任何 Page 方法 | ❌ |

CouponServiceImpl 的 6 个列表方法仍返回无界 `List<>`。Controller 层也无分页端点（V1AdminMarketingController / V1MerchantMarketingController 均无 `listCoupons` 方法）。

---

### ⚠️ #13 ThreadLocal 异步泄漏 — common/ 修了，util/ 未修

| 文件 | 之前 | 之后 | 状态 |
|------|------|------|------|
| `common/TenantContextHolder.java` | `ThreadLocal` | `InheritableThreadLocal` | ✅ |
| `util/TenantContextHolder.java` | `ThreadLocal` | `ThreadLocal`（未变） | ❌ |

`util/` 版本功能更丰富（带 Sa-Token 回退），如果这是主用版本则修复不完整。`common/` 版本注释明确指出仍需 TaskDecorator 兜底。

---

### ⚠️ #16 库存扣减无幂等 — 未变

`ProductInventoryServiceImpl.deductStock` 仍只将 `bizNo` 用于异常消息，不做去重。消息级幂等在 PaymentV1Consumer 已兜底，此项降级为低风险。

---

## 审查结论（二轮后）

P1 12 项中 **10 项完全修复，2 项部分修复**。

### 剩余必做

| # | 问题 | 工作量 |
|---|------|--------|
| #12 | CouponServiceImpl 加分页（6 个公开方法 + Controller 端点） | ~80行 |
| #13 | util/TenantContextHolder 改为 InheritableThreadLocal | 1行 |

### 建议（低风险）

| # | 问题 | 说明 |
|---|------|------|
| #13 | 加 TaskDecorator 真正兜底线程池复用场景 | ~30行 |
| #16 | deductStock 内部加 bizNo 去重 | 消息级幂等已兜底 |
