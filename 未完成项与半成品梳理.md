# 未完成项与半成品梳理

## 1. 说明

这份文档基于以下两部分内容整理：

- 需求总览：`项目方向与需求规划.md`
- 当前代码仓库：`payment-system`、`salessystem`

目的不是重复功能清单，而是把当前项目里：

- 还没有真正开始写的能力
- 已经有接口名、页面壳子或基础设施，但还没有真正完成的能力
- 已经有主干、但还存在明显缺口的能力

集中整理出来，方便后续排期。

在当前阶段，这份文档还额外承担一个作用：

- 明确“下一步先做什么”
- 把最高优先级事项收敛成可直接执行的开发任务
- 避免任务继续发散到优惠券、AI、登录扩展等后续能力

---

## 2. 完全没写或基本没落地的部分

### 2.1 优惠券体系

需求里已经明确把优惠券作为重点能力提出，包括：

- 优惠券类型
- 领取、发放、锁定、核销、过期
- 与余额、积分叠加规则

对应需求位置：

- `项目方向与需求规划.md` 第 3.4 节
- `项目方向与需求规划.md` 第 7 节

当前代码现状：

- 在 `payment-system/src` 和 `salessystem/src` 中几乎找不到成体系的 `coupon / 优惠券 / voucher` controller、service、entity、page
- 前端 AI 页面里提到过“优惠券”文案，但不是功能实现
- 但数据库设计层已经存在 `payment-system/sql/15_coupon_marketing.sql`
- 已设计的对象包括：`coupon_template`、`user_coupon`、`coupon_operation_log`、`order_coupon_detail`、`marketing_activity`、`activity_grant_record`

结论：

- 这块更准确的状态是“库表与模型设计已起草，业务服务、接口与页面基本未落地”

### 2.2 会员等级、会员标签

需求里提到了：

- 会员等级
- 会员标签

对应需求位置：

- `项目方向与需求规划.md` 第 7 节

当前代码现状：

- 数据库设计层已经存在 `payment-system/sql/18_store_membership.sql`
- 已设计的对象包括：`member_level`、`member_tag`、`member_tag_relation`、`member_growth_log`
- 但没看到独立的会员等级、会员标签业务接口
- 没看到前端对应管理页面或用户展示链路

结论：

- 这块更准确的状态是“会员主数据设计已存在，但服务、接口、页面基本未开始”

### 2.3 账号绑定 / 解绑 / 找回密码 / 手机号注册

需求里提到了：

- 手机号注册
- 找回密码
- 一个账号绑定手机号 / 微信 / GitHub
- 首次第三方登录自动注册
- 已有账号与第三方身份绑定

对应需求位置：

- `项目方向与需求规划.md` 第 5.1 节
- `项目方向与需求规划.md` 第 5.3 节

当前代码现状：

- 用户端已有基础注册、密码登录
- 但没有看到完整的“绑定、解绑、找回密码、手机号注册、第三方绑定”模块

结论：

- 当前只完成了最基础的一小部分，完整账号体系还没有落地

### 2.4 商家端和管理端的短信登录、第三方登录

需求里提到：

- 商家账号登录、第三方登录、手机号短信验证
- 管理员登录、权限控制

对应需求位置：

- `项目方向与需求规划.md` 第 2.2 节
- `项目方向与需求规划.md` 第 5 节

当前代码现状：

- 商家端目前只有 `POST /v1/merchant/auth/login`
- 管理端目前只有 `POST /v1/admin/auth/login`
- 都是账号密码登录，没有短信登录和第三方登录链路

结论：

- 商家端和管理端的扩展登录方式还没做

### 2.5 平台级活动管理

需求里提到了：

- 平台级活动管理
- 商户活动与平台活动的统一运营能力

对应需求位置：

- `项目方向与需求规划.md` 第 2.3 节
- `项目方向与需求规划.md` 第 3.4 节
- `项目方向与需求规划.md` 第 7 节

当前代码现状：

- 数据库设计层已经存在 `payment-system/sql/15_coupon_marketing.sql`
- 其中 `marketing_activity` 已明确区分 `PLATFORM` 和 `TENANT` 活动范围
- 但当前未看到成体系的平台活动 controller、service、管理页面

结论：

- 这块目前应视为“营销活动数据结构已设计，但平台级活动管理能力未真正落地”

### 2.6 登录安全能力

需求里提到了：

- 短信验证码有效期与防刷机制
- 登录失败限制
- 异地登录提醒
- Token / Session 管理
- 权限隔离与角色控制

对应需求位置：

- `项目方向与需求规划.md` 第 5.4 节

当前代码现状：

- 数据库设计层已经存在 `payment-system/sql/17_auth_security.sql`
- 已设计的对象包括：`sms_verify_code`、`login_session`、`user_login_log`、`login_fail_record`、`password_reset_record`
- 但当前梳理到的真实业务落地，仍主要停留在基础登录主链，并未看到完整的验证码、防刷、失败锁定、异常登录提醒闭环

结论：

- 这块不应只归类为“登录方式未补完”，还应视为“安全能力已设计但业务未落地”

---

## 3. 写了接口名或框架，但没有真正实现的部分

### 3.1 用户短信登录、第三方登录目前仍是“同一套密码登录”

前端已经区分了三种调用：

- `/v1/app/auth/login/password`
- `/v1/app/auth/login/sms`
- `/v1/app/auth/login/third-party`

但当前实际情况是：

- 前端登录页直接写明“短信验证码当前仍复用 `password` 字段”
- 文件：`salessystem/src/pages/Login.tsx:102`
- 后端 `V1AppAuthController` 里这三条最终都还是调用同一个 `platformIdentityService.login(dto)`
- 文件：`payment-system/src/main/java/com/payment/controller/V1AppAuthController.java`

结论：

- 目前只是“接口路径分开了”，不是“真实业务逻辑分开了”

### 3.2 微信登录 / 第三方登录仍是占位实现

`UserServiceImpl` 中仍然保留了明确的 TODO：

- `payment-system/src/main/java/com/payment/service/impl/UserServiceImpl.java:128`

内容指向：

- 实际项目需要调用微信 API 验证 code 并获取 openid

说明：

- 这表示当前第三方登录还没有接真实的微信授权换取身份流程

结论：

- 这是典型的“骨架已写，核心步骤未完成”

### 3.3 购物车真实下单闭环已完成，当前转为订单后链路收尾

本轮会话已完成：

- 商品详情页已支持“加入购物车”和“立即购买”
- 购物车页已从本地 mock 改为真实状态层
- 购物车支持按商户分别创建真实订单
- 下单后已复用真实 `/payment/status`
- 未登录场景下已明确跳转到登录页

对应代码落点：

- `salessystem/src/context/CartContext.tsx`
- `salessystem/src/services/orderCheckout.ts`
- `salessystem/src/pages/ProductDetails.tsx`
- `salessystem/src/pages/Cart.tsx`

当前结论：

- “商品详情 -> 购物车 -> 创建真实订单 -> 支付状态页”主闭环已经打通
- 当前用户交易链路的主要缺口，已从“购物车下单”转移为“订单后链路收尾”

### 3.4 AI 助手前端仍是演示页面，不是真实联调

`AIAssistant.tsx` 当前表现为：

- 首屏内容就是固定写死的示例消息
- 文件：`salessystem/src/pages/AIAssistant.tsx:43`
- 发送消息后通过 `setTimeout` 模拟 AI 回复
- 文件：`salessystem/src/pages/AIAssistant.tsx:64`

结论：

- 这是展示型页面，不是真正的 AI 产品链路

### 3.5 AI 后端接口有雏形，但还比较粗糙

后端已有：

- `AiConrtoller`
- `DataAnalysisController`
- `DataAnalysisServiceImpl`

相关文件：

- `payment-system/src/main/java/com/payment/controller/AiConrtoller.java`
- `payment-system/src/main/java/com/payment/controller/DataAnalysisController.java`
- `payment-system/src/main/java/com/payment/service/impl/DataAnalysisServiceImpl.java`

但当前仍有明显问题：

- `AiConrtoller` 的 `/recommend/products` 没定义路径参数，却在方法里用 `@PathVariable`
- 文件：`payment-system/src/main/java/com/payment/controller/AiConrtoller.java:27`
- 这更像半成品接口，而不是稳定可用接口

结论：

- AI 后端不是完全空白，但距离可稳定使用还有一段距离

### 3.6 支付宝订单查询还没实现

明确 TODO：

- `payment-system/src/main/java/com/payment/service/impl/PaymentServiceImpl.java:359`

结论：

- 支付能力不是全空，但支付宝查询链路明显没有收尾

### 3.7 退款链路已起框架，但部分 provider 仍不支持

在 `RefundServiceImpl` 中明确出现：

- `Provider refund is not supported in phase 1`
- 文件：`payment-system/src/main/java/com/payment/service/impl/RefundServiceImpl.java:146`

说明：

- 退款补偿、对账、晚到回调处理这些框架已经有了
- 但至少某些支付渠道的真实退款提交/查询能力还没完全落地

结论：

- 这是“基础架构先搭好，渠道实现后补”的典型状态

### 3.8 RabbitMQ 和异步补偿不是没做，但还没有完全做完

项目里已经有：

- RabbitMQ 配置
- 死信消费者
- 支付成功消费队列
- 商品索引异步队列
- 补偿任务实体
- 幂等记录、消息外盒、退款补偿任务

这次已补上的链路：

- 商品主服务不再直接同步调用搜索服务写 ES
- 商户侧商品新增、修改、删除也会发商品索引消息
- 商品索引改为通过 `payment.product.index` 异步消费更新/删除 ES 文档
- `ProductServiceImpl <-> ProductSearchServiceImpl` 的循环依赖已拆开

但 `OrderConsumer` 中仍保留明显 TODO：

- 订单创建后的业务处理未实现
- 文件：`payment-system/src/main/java/com/payment/consumer/OrderConsumer.java:167`
- 订单支付成功后的业务处理未实现
- 文件：`payment-system/src/main/java/com/payment/consumer/OrderConsumer.java:190`

TODO 内容里提到了：

- 发送通知
- 更新统计
- 记录行为日志
- 触发后续流程

结论：

- MQ 设施已经在路上
- 但还没有把需求里提到的“统一重试、补偿、异步闭环”完整覆盖完

### 3.9 RabbitMQ 的适用范围在需求里写得更大，但当前只落了一部分

需求里明确把 RabbitMQ 扩展到了：

- 支付回调处理
- 充值到账补偿
- 订单超时关闭
- 退款结果补查
- 优惠券发放与核销补偿
- 积分发放补偿
- 短信发送失败重试
- 第三方登录绑定异步处理
- AI 分析任务与结果回写

对应需求位置：

- `项目方向与需求规划.md` 第 4.1 节
- `项目方向与需求规划.md` 第 6.4 节

当前代码现状：

- 已存在 `payment-system/sql/19_message_retry.sql`
- 已设计的对象包括：`message_consume_log`、`retry_task`
- 当前项目里最明显已开始建设的是支付、退款、补偿、死信、消息外盒
- 但还没有看到短信重试、第三方绑定异步处理、优惠券补偿、AI 任务回写这些链路的成体系实现

结论：

- 当前 MQ 体系不是没做，而是“支付链路优先落地，需求中更大范围的统一消息治理尚未覆盖完”

---

## 4. 已有主干，但数据或页面仍不完整的部分

### 4.1 管理端和商户端部分页面已经接真接口，但字段不够完整

前端页面自己已经写出了当前缺口：

- 商户详情接口没有直接返回商户钱包余额
- 文件：`salessystem/src/pages/AdminMerchantDetail.tsx:209`
- 用户详情接口没有返回消费日志、积分流水等明细
- 文件：`salessystem/src/pages/AdminUserDetail.tsx:166`
- 商户财务页没有独立的真实收支流水接口
- 文件：`salessystem/src/pages/merchant/MerchantFinance.tsx:345`

结论：

- 这些模块不算没做
- 但距离“完整经营视图 / 治理视图”还有接口补齐工作

### 4.2 商品详情页已接入真实交易动作，但订单重试链路仍可继续补完

本轮会话已完成：

- 商品详情页不再只停留在浏览展示
- 已可直接加入购物车
- 已可直接触发真实下单
- 支付状态页已补齐失败、关闭、过期场景的统一状态文案
- 订单页与支付状态页已能提示“当前复用旧支付单 / 当前新建支付单”

当前剩余缺口：

- 当前已能把失败、关闭、取消后的订单重新引导回购物车与订单详情
- 后续如果还要继续补强，重点会从“再次拉起收口”转向“更细的转化体验优化”

### 4.3 用户端订单取消能力已补齐，当前转为列表与支付体验补强

本轮会话已完成：

- 订单详情页已支持取消订单
- 订单列表页已支持取消待支付订单
- 取消后会刷新真实订单状态

对应代码落点：

- `salessystem/src/pages/UserOrderDetail.tsx`
- `salessystem/src/pages/UserOrders.tsx`

当前结论：

- 这项不再属于“接口在，页面没收尾”
- 目前已进入“列表筛选、支付重试和体验补强”阶段

### 4.4 用户侧搜索、分类、收藏、活动信息仍多为展示壳子

需求里提到了：

- 浏览多个商户店铺、商品、活动、优惠信息
- 搜索商户、商品、分类
- 收藏、偏好、推荐等行为沉淀

对应需求位置：

- `项目方向与需求规划.md` 第 2.1 节
- `项目方向与需求规划.md` 第 6.1 节

当前代码现状：

- `salessystem/src/pages/Home.tsx` 中搜索框、分类入口、领券中心等更像展示入口
- `salessystem/src/pages/Discovery.tsx` 中搜索、筛选、分类切换还没有真正和后端参数联动
- `salessystem/src/pages/PublicMerchantDetail.tsx` 中“收藏店铺”“店内搜索”“商户信息/联系方式”仍偏页面壳子
- 虽然已有商品分类字段、浏览行为日志等基础，但还没有真正形成“搜索 -> 分类 -> 收藏 -> 活动信息”的完整用户消费发现链路

结论：

- 这块不是完全空白，但目前仍偏“可展示、未深度可用”

### 4.5 门店 / 店铺主数据与多门店经营能力已设计，但业务层未落地

需求里提到了：

- 店铺信息管理
- 多租户经营中的门店信息、联系方式、营业时间、服务标签

对应需求位置：

- `项目方向与需求规划.md` 第 2.2 节
- `项目方向与需求规划.md` 第 3.1 节

当前代码现状：

- 数据库设计层已经存在 `payment-system/sql/18_store_membership.sql`
- 已设计 `store` 表，包含营业时间、地址、经纬度、服务标签等信息
- 但当前前后端主链仍主要围绕 `tenant / merchant` 展开，没有看到完整的门店主数据管理、门店列表、门店维度经营能力

结论：

- 这块应视为“门店模型已设计，但真实业务仍停留在商户级”

### 4.6 账户边界与资产边界在需求层强调过，但当前文档里还没有单独收口

需求里反复强调：

- 平台统一用户体系 vs 商户独立经营体系
- 商户内余额 vs 平台钱包
- 跨商户通用储值卡是否存在

对应需求位置：

- `项目方向与需求规划.md` 第 3.1 节
- `项目方向与需求规划.md` 第 3.3 节
- `项目方向与需求规划.md` 第 10 节

当前代码现状：

- 当前已经有统一钱包与商户钱包两套能力雏形
- 但“最终以哪种资产边界为主”“哪些场景允许跨商户复用”仍更像需求层待明确事项

结论：

- 这不是单纯的代码缺口，也属于需要优先收口的产品与建模边界问题

---

## 5. 当前已经具备、可以作为继续开发基础的部分

虽然上面列了不少缺口，但也有一批主干已经具备，可以作为下一阶段的开发基础：

- 三端基础登录主链已经跑通
- `AuthContext`、token、本地 session 管理已经成型
- 用户端浏览、钱包、充值、订单、支付状态主链基本都有真实接口
- 商户端商品、订单、提现、积分规则、充值规则已有较完整接入
- 管理端用户、商户、订单、支付单、充值单、提现审核已有较完整接入
- 支付回调、幂等、补偿、退款对账这些基础设施已经开始建设
- 优惠券、营销活动、会员等级、会员标签、登录安全这些方向已经出现数据库设计草稿

结论：

- 当前项目不是“从零开始”
- 更像是“主干已成型，但第二阶段运营能力和部分收尾还没补完”

---

## 6. 本轮会话已完成的修改

本轮会话已经完成以下改动：

1. 打通 `商品详情 -> 购物车 -> 创建真实订单 -> 支付状态页`
2. 补齐订单详情页查看支付状态的真实跳转
3. 补齐订单详情页取消订单交互
4. 补齐订单列表页筛选、继续支付、取消订单、查看详情操作
5. 补齐同一订单下“先复用有效支付单、无有效单再新建”的继续支付链路
6. 补齐支付失败、支付关闭、支付过期的统一状态文案
7. 补齐订单页与支付状态页对“复用旧支付单 / 新建支付单”的用户提示
8. 补齐已关闭 / 已失败订单的“重新购买同款商品”与再次拉起收口动作

对应代码落点：

- `salessystem/src/context/CartContext.tsx`
- `salessystem/src/services/orderCheckout.ts`
- `salessystem/src/pages/ProductDetails.tsx`
- `salessystem/src/pages/Cart.tsx`
- `salessystem/src/pages/UserOrderDetail.tsx`
- `salessystem/src/pages/UserOrders.tsx`
- `salessystem/src/pages/PaymentStatus.tsx`
- `salessystem/src/utils/orderActions.ts`
- `salessystem/src/utils/paymentStatus.ts`
- `payment-system/src/main/java/com/payment/dto/SalesOrderDetailVO.java`
- `payment-system/src/main/java/com/payment/dto/OrderPaymentVO.java`
- `payment-system/src/main/java/com/payment/controller/V1AppOrderController.java`
- `payment-system/src/main/java/com/payment/service/AppOrderService.java`
- `payment-system/src/main/java/com/payment/service/PaymentBillV1Service.java`
- `payment-system/src/main/java/com/payment/service/impl/PaymentBillV1ServiceImpl.java`
- `payment-system/src/main/java/com/payment/service/impl/AppOrderServiceImpl.java`
- `payment-system/src/test/java/com/payment/service/impl/AppOrderServiceImplTest.java`

本轮完成后的结果：

- 用户已可从真实商品详情进入真实购物车并创建真实订单
- 用户已可在订单详情和订单列表中继续支付或取消待支付订单
- 订单详情已可拿到 `paymentBillNo` 并正确跳转支付状态页
- 用户继续支付前，后端会先检查同一订单下是否仍存在有效支付单；仅在没有有效支付单时才新建支付单
- 支付状态页已能区分支付成功、支付中、支付失败、支付关闭、支付过期等主要感知状态
- 用户继续支付后，前端已能明确提示本次是“复用旧支付单”还是“新建支付单”
- 用户在订单列表、订单详情、支付状态页中，已可把失败或关闭订单重新收口到“重新购买同款商品”
- 原订单商品已可直接重新加入购物车，用户无需手动回找商品

### 6.1 本轮已完成的验收结果

已确认完成：

- 商品可从真实商品详情加入购物车
- 购物车数据不再是页面本地 mock
- 用户可基于购物车创建真实订单
- 订单创建后可进入支付状态页
- 未登录场景下有明确跳转到登录页的处理
- 用户可在订单详情和订单列表中取消待支付订单
- 用户可在订单详情和订单列表中继续进入支付状态页
- 用户重新进入支付前，会优先复用同一订单下仍有效的支付单
- 仅当同一订单下不存在有效支付单时，才会新建支付单
- 订单取消后，会关闭该订单下所有未成功支付单，避免遗留悬挂支付单
- 支付失败、支付关闭、支付过期场景的前端状态文案已统一
- 用户在继续支付后，可感知本次是否复用了旧支付单
- 已关闭、已取消、支付失败订单已可从订单页快速进入重新购买路径
- 支付状态页在失败、关闭、过期场景下已补齐更直接的去向分流

当前仍明确未做：

- 不做优惠券抵扣
- 不做后端购物车表
- 不做跨租户合单
- 不做用户短信登录真实验证码流程
- 不做用户第三方登录真实 OAuth / openid 流程
- 不做商户端 / 管理端登录扩展
- 不做 AI 助手联调和支付渠道深度补完

---

## 7. 下一步建议

### 7.1 已完成：支付失败后的支付体验与状态治理

本轮已完成：

1. 支付失败或支付单关闭后的用户提示细化
2. 订单页与支付状态页对“当前复用旧支付单 / 当前新建支付单”的提示补强
3. 支付过期、支付失败、支付关闭的前端状态文案统一

完成后的意义：

- 当前已能创建订单、继续支付、取消待支付订单
- 同一订单下“先复用有效支付单、无有效单再新建”的后端链路已经补齐
- 用户感知层面的支付状态治理已不再是主要缺口

### 7.2 已完成：已关闭 / 已失败订单的再次拉起收口

本轮已完成：

1. 在订单列表中把“已关闭 / 已取消 / 支付失败”订单的主操作改成更明确的“重新下单”或“重新购买”
2. 在订单详情页根据当前状态补“重新购买同款商品”或“返回商品详情”的清晰入口
3. 支付状态页在失败、关闭、过期场景下补更直接的去向分流，而不是只给单一路径返回

完成后的意义：

- 7.1 已经把状态提示和支付单来源提示补齐
- 当前真正还会影响用户成交闭环的“失败后下一步该点哪里”问题，已经基本被收口
- 用户端交易链路已从“支付状态感知不清”进一步推进到“失败后可继续重购”

### 7.3 现在最适合继续补：登录与账号体系补完

建议优先做：

1. 用户短信登录真实验证码流程
2. 用户第三方登录真实 OAuth / openid 流程
3. 商户端 / 管理端登录扩展
4. 账号绑定 / 解绑 / 找回密码 / 手机号注册

原因：

- 当前用户端下单、继续支付、取消订单、失败后重购已经具备较完整闭环
- 再往后继续补支付体验，收益会开始递减
- 登录与账号体系仍是需求里明确存在、但当前完成度偏低的一大块主链

### 7.4 再下一阶段：经营与运营能力

继续推进：

1. 优惠券体系
2. 会员等级 / 标签
3. 完整积分运营规则

### 7.5 最后阶段：增强与智能化

最后再处理：

1. AI 助手真实接口联调
2. 商家智能客服
3. 平台分析型 AI
4. 更完整的 MQ 异步补偿覆盖
5. 更完整的退款与支付渠道能力

---

## 8. 一句话总结

当前项目已经不再停留在“购物车和订单是半成品”的状态：本轮会话已把用户端真实下单、订单支付状态、继续支付、取消订单、失败提示和失败后重购路径补到了可用水平；下一步最值得继续补的是登录与账号体系。
